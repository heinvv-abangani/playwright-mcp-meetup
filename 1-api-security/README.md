## 🚀 Quick Start (5 Minutes)

```bash
npx http-server -p 8080
```

Visit http://localhost:8080 to verify it's running.

Prompt:

`Use Playwright MCP to study localhost:8080.

Audit all APIs on this localhost port.`
