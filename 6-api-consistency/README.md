# Playwright MCP Network Interception Demo

## 🚀 Quick Start

### 1. Install and Run

```bash

# Install dependencies
npm install

# Start the demo API
npm start
```

The demo API will start on `http://localhost:3000`

### 2. Run Playwright MCP

Prompt: 

`Use playwright mcp to study localhost:3000 data.

Audit all APIs on localhost:3000.`
